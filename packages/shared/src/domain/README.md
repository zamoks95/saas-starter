# Domain

Shared domain primitives used across both frontend and backend.

## Structure

- `value-objects/` - Shared value objects (e.g., Email, Money, UserId) with Zod schemas
- `errors/` - Base domain error types
